{% extends 'admin/layout.html.twig' %}

{% block title %}Editar Parceiro | DuoFilms{% endblock %}
{% block page_title %}Editar Parceiro: {{ partner.name }}{% endblock %}

{% block content %}
    <div class="bg-gray-800 rounded-lg shadow-lg p-6">
        {{ include('admin/partner/_form.html.twig', {'button_label': 'Atualizar'}) }}

        <div class="flex justify-between items-center mt-8">
            <a href="{{ path('app_partner_index') }}" class="text-blue-400 hover:text-blue-300 flex items-center">
                <i class="fas fa-arrow-left mr-2"></i>
                <span>Voltar à lista</span>
            </a>

            {{ include('admin/partner/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}