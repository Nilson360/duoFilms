{% extends 'admin/layout.html.twig' %}

{% block title %}Editar Vídeo | DuoFilms{% endblock %}
{% block page_title %}Editar Vídeo: {{ video.title }}{% endblock %}

{% block content %}
    <div class="bg-gray-800 rounded-lg shadow-lg p-6">
        {{ include('admin/video/_form.html.twig', {'button_label': 'Atualizar'}) }}

        <div class="flex justify-between items-center mt-8">
            <a href="{{ path('app_video_index') }}" class="text-blue-400 hover:text-blue-300 flex items-center">
                <i class="fas fa-arrow-left mr-2"></i>
                <span>Voltar à lista</span>
            </a>

            {{ include('admin/video/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}