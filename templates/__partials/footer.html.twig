<footer class="bg-[#121212] mt-4">
    <hr class="w-full border-t border-white/10 my-6">
    <div class="w-full max-w-screen-xl mx-auto p-4 md:py-8">
        <div class="sm:flex sm:items-center sm:justify-between">
            <a href="{{ path('app_home') }}" class="flex items-center mb-4 sm:mb-0 space-x-3 rtl:space-x-reverse">
                <img src="{{ asset('images/logo.png') }}" class="h-8" alt="Duo Films Logo"/>
                <span class="self-center text-2xl font-semibold whitespace-nowrap text-white">Duo Films</span>
            </a>
            <ul class="flex flex-wrap items-center mb-6 text-sm font-medium text-white sm:mb-0">
                <li>
                    <a href="{{ path('app_footer_content', {slug:'about'} ) }}" class="hover:underline me-4 md:me-6">Sobre
                        nós</a>
                </li>
                <li>
                    <a href="{{ path('app_footer_content', {slug:'privacy'}) }}" class="hover:underline me-4 md:me-6">Política
                        de Privacidade</a>
                </li>
            </ul>
        </div>
        <span class="block text-sm text-white sm:text-center">
            © {{ "now"|date("Y") }} <a href="{{ path('app_footer_content', {slug:'about'}) }}" class="hover:underline">Duo Films™</a>. Todos os direitos reservados.
        </span>
    </div>
</footer>
